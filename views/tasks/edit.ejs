<!DOCTYPE html>
<html>
  <head>
    <title>Edit Task</title>
    <link rel='stylesheet' href="https://bootswatch.com/4/lux/bootstrap.min.css" />
  </head>
  <body>
    <div class="container">
      <h3><a href="/">Task List</a></h3>
      <h1>Edit Task</h1>
      <form action="/tasks/update/<%= task._id%>" method="post">
        <table>
          <tbody>
            <tr>
              <td>Title</td>
              <td><input type="text" name="title" value="<%= task.title %>" /></td>
            </tr>
            <tr>
              <td>Content</td>
              <td><textarea name="content"><%= task.content %></textarea></td>
            </tr>
            <tr>
              <td colspan="2"><button type="submit">Save Changes</button></td>
            </tr>
          </tbody>
        </table>
      </form>
    </div>
  </body>
</html>
